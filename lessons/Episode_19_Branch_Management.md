# Episode 19: Branch Management

- To create a branch within a custom branch, first switch to that branch, then create the new branch.
- Use `git branch -m <new-branch-name>` to rename the current branch.